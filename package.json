{
  "name": "firebase-rules-tests",
  "version": "1.0.0",
  "description": "Firebase rules testing via Emulator",
  "private": true,
  "scripts": {
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.2",
    "firebase-admin": "^12.0.0",
    "firebase-tools": "^13.0.0",
    "jest": "^29.7.0"
  }
}
